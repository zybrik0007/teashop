import { ModaltransferDirective } from './modaltransfer.directive';

describe('ModaltransferDirective', () => {
  it('should create an instance', () => {
    const directive = new ModaltransferDirective();
    expect(directive).toBeTruthy();
  });
});
